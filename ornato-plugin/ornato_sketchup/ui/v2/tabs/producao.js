/**
 * Tab placeholder — Fase 1.1
 * Cada tab vai ganhar render() próprio na Fase 2 (Engenharia de modelagem).
 * Por enquanto retorna nada — app.js usa renderEmptyTab quando module não tem render().
 */
export const meta = { phase: 'F1.1' }
